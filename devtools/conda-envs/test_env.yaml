name: test
channels:
  - conda-forge
  - defaults
  - tpeulen
dependencies:
  # Base depends
  - python>=3.8
  - pip
  # Package depends
  - numpy
  - pandas
  - biopandas
  - seaborn
  - scipy
  - jupyter
  - jupyterlab
  - pymol-open-source
  # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval
  # Pip-only installs
  - pip:
    - flatten-dict
