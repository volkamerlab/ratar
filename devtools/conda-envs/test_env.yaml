name: test
channels:
  - conda-forge
  - defaults
  - tpeulen
dependencies:
  # Base depends
  - python>=3.6
  - pip
  # Package depends
  - numpy
  - pandas
  - biopandas
  - seaborn
  - scipy
  - jupyter
  - jupyterlab
  - pymol-open-source
  # Testing
  - pytest
  - pytest-cov
  - codecov
  # Pip-only installs
  - pip:
    - flatten-dict
